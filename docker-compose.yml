version: '3.8'

services:
  inspiremusic:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: inspiremusic
    ports:
      - "3000:3000"
    restart: unless-stopped
    networks:
      - inspiremusic-network

networks:
  inspiremusic-network:
    driver: bridge

